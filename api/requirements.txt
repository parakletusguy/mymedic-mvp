# ── MyMedic API Dependencies ──────────────────────────────────
# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1
pydantic[email]==2.9.0
pydantic-settings==2.5.0

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.0

# Security / Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
pyotp==2.9.0

# Email (placeholder – swap for production SMTP)
aiosmtplib==3.0.1
jinja2==3.1.4

# Encryption (Fernet for message content at rest)
cryptography==43.0.0

# HTTP Client (Paystack API calls)
httpx==0.27.0

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.0
pytest-asyncio==0.24.0
aiosqlite==0.20.0         # Async SQLite for test DB (no Postgres needed)
respx==0.21.1             # Mock httpx requests (Paystack)
